import type { ButtonProps } from '@omnific/atelier';
import { Button } from '@omnific/atelier';

/**
 * 展示 Button 的 danger 变体。
 */
export const ButtonDangerExample = (props: ButtonProps) => <Button {...props} />;
