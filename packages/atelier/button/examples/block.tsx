import type { ButtonProps } from '@omnific/atelier';
import { Button } from '@omnific/atelier';

/**
 * 展示占满父容器宽度的 Button。
 */
export const ButtonBlockExample = (props: ButtonProps) => <Button {...props} />;
